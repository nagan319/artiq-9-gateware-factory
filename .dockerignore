# Don't send build outputs or scratch dirs to the Docker daemon
output/
input/
*.log

# Uncomment to exclude the installer from the build context once vivado-2024.2-env
# is already built — saves time when rebuilding artiq9-builder only.
# FPGAs_AdaptiveSoCs_Unified_2024.2_1113_2356_Lin64.bin
